# Supabase Configuration
# Get these values from your Supabase project dashboard: https://app.supabase.com/

# Public Supabase URL (safe to expose in client-side code)
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url-here

# Public Supabase Anonymous Key (safe to expose in client-side code)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Service Role Key (KEEP SECRET - only use server-side)
# WARNING: This key bypasses Row Level Security. Never expose to client!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Security Configuration (Phase 5)

# CORS Configuration
# Comma-separated list of allowed origins for CORS
ALLOWED_ORIGINS=https://lenkersdorfer-crm.vercel.app,https://www.lenkersdorfer-crm.vercel.app

# Business Configuration

# Default commission rate (percentage, optional override)
# DEFAULT_COMMISSION_RATE=15

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace the placeholder values with your actual Supabase credentials
# 3. Update ALLOWED_ORIGINS with your actual deployment URLs
# 4. Never commit .env.local to git
# 5. For Vercel deployment, add these as environment variables in the Vercel dashboard
